{
  "name": "@stele/cors-proxy",
  "version": "0.1.0",
  "description": "Stele CORS proxy — Cloudflare Worker that fetches .stele artifacts from sources that don't allow cross-origin requests.",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc -b"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250131.0",
    "typescript": "~6.0.2",
    "wrangler": "^3.108.0"
  }
}
